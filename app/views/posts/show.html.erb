<h2><%= @post.title %></h2>
<%= link_to "@#{@user.username}", user_path(@user.id) %>
<br>
<%= @post.body %>
<br>
<span class="posttime"><%= @post.created_at %></span>
<br>
<%= link_to "Delete Post", user_post_path(@user.id, @post.id), method: :delete, confirm: "Are you sure you want to delete this post?" %>
<br><br>
<span class="myposts"><%= link_to "My Posts", user_posts_path(@user.id) %></span>
<br>
<%= link_to "Delete #{@user.username}", user_path(@user.id), method: :delete, confirm: "Are you sure you want to delete @#{@user.username}?" %>
<br><br>
<hr>