<h1>All Users</h1>

<% @users.each do |user| %>
	<span class="username"><%= link_to "@#{user.username}", user_path(user.id) %></span>
	<br>
	<%= link_to "Delete User", user_path(user.id), method: :delete, confirm: "Are you sure you want to delete #{user.username}?" %>
	<br><br>
<% end %>